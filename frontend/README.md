# NovaChat Frontend (React)

Simple React + Vite chat UI for the Spring Boot backend.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open the URL shown in the terminal (usually `http://localhost:5173`).

Make sure the backend is running on `http://localhost:8080`.
